# All Python dependencies have been moved to backend/requirements.txt
# Install with: pip install -r backend/requirements.txt

# Or run from backend directory:
# cd backend && pip install -r requirements.txt

